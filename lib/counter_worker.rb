class CounterWorker
  
  def perform
    # count = 0 
    # File.readlines('public/words.txt').each do |line|
    #   line.split(" ").each do |word|
    #     count+=1
    #   end
    # end
    # t = Time.now - time
    # # Statistic.create(time_taken: t)
    # Rails.configuration.x.taken_time += t
    # puts "taken time#{Rails.configuration.x.taken_time}" + '\n'
    # puts "Interval:  #{t}"
  end 

end